# Decisions for FEAT-20260918-001

## Idea

供应商管理：维护供应商档案（供应商编码、名称、联系人、联系电话、状态启用/停用、备注），支持按名称和状态分页查询、新增、修改、删除和导出 Excel；菜单挂在新的『业务管理』目录下，列表/新增/修改/删除/导出按钮级权限控制。后端接口和前端页面都要有。

## Product revisions

- r1 (2026-09-18T02:34:50.279862Z): 决策如下，请据此清空 questions 并更新规则与验收条件：Q1 编码由用户手工录入，必填，系统内唯一；Q2 编码创建后不可修改；Q3 名称不要求唯一；Q4 联系人和联系电话都选填，不校验格式；Q5 逻辑删除（del_flag），无删除限制，删除后不再出现在列表和详情中；Q6 新增默认启用；Q7 导出当前查询条件下的全部数据，不分页；Q8 名称模糊匹配（包含即命中）；Q9 不提供快捷开关；Q10 不做部门数据权限隔离；Q11 查询条件只有名称和状态。状态取值沿用系统字典 sys_normal_disable（0=正常/启用，1=停用）。验收条件调整：A3 和 A23 删除（受限用户的导航和按钮可见性本期不验收）；A22 改为 automated，通过系统的角色和用户接口创建一个只有供应商列表权限的测试用户，用该用户调用新增、修改、删除、导出接口并断言被拒绝；只保留 A21 为 manual，其余全部 automated。
- r2 (2026-09-18T02:35:57.529791Z): Q12 决策：可以复用，编码只在未删除的供应商中唯一。请清空 questions，其余保持不变。

## Clarifications

- none

## Answers to agent questions

- none

## Manual acceptance

- A21 (2026-09-18T03:43:57+00:00): 用户手动核对：Excel 导出各列与系统一致，状态显示为正常/停用

## Resets

- none
