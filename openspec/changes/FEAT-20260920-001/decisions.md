# Decisions for FEAT-20260920-001

## Idea

采购单管理（主子表）：采购单主表包含单号、供应商（从供应商档案中选择，只能选启用状态的供应商）、下单日期、状态（草稿/已提交）、备注、合计金额；明细子表包含物料名称、数量、单价、金额（数量×单价），合计金额等于明细金额之和。支持主子表一起新增和修改、按单号/供应商/状态/日期范围分页查询、查看详情、删除、提交（草稿变已提交，已提交的单据不能再修改和删除）、导出 Excel。菜单挂在『业务管理』目录下，按钮级权限控制。后端接口和前端页面都要有。

## Product revisions

- r1 (2026-09-20T07:02:13.024293Z): 决策：Q1 选 1（系统自动生成单号，前缀 PO + 日期 + 流水号，用户不可编辑）；Q2 选 1（已提交即终态，不允许撤回或作废）；Q3 选 1（仅导出主表，一张单据一行，含合计金额）；Q4 选 1（数量为正整数，单价保留 2 位小数，均必须大于 0）；Q5 选 3（供应商被停用后单据可查看和提交，仅在修改保存时要求改选启用状态的供应商）；Q6 选 1（所有拥有菜单权限的用户都能查询全部采购单，不做数据权限隔离）。请清空 questions，其余保持不变；A27 保持 manual，其余保持 automated。

## Clarifications

- none

## Answers to agent questions

- none

## Manual acceptance

- A27 (2026-09-20T09:47:49+00:00): 用户手动核对：导出 Excel 列标题为中文，状态显示草稿/已提交，数据行与页面查询结果一致

## Resets

- 2026-09-20T07:59:43+00:00: from WORKSPACE_TAINTED at attempt 1 — 实现代理在 1800 秒超时时被中断（主子表一轮实现超过 30 分钟）；超时已提高到 3600 秒，保留已写出的文件重试
- 2026-09-20T08:16:44+00:00: from WORKSPACE_TAINTED at attempt 1 — 实现代理两次在约 139 条消息时被 --max-turns 60 截断（非超时）；上限提到 200 后重试，保留已写出的文件
- 2026-09-20T08:28:55+00:00: from WORKSPACE_TAINTED at attempt 1 — 前端构建生成的 auto-imports.d.ts / components.d.ts 未声明为 scratch；已加入 scratch_paths 后重试
